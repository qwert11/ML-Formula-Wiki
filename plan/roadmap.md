# Roadmap реализации

> Roadmap — это только порядок выполнения задач. Требований и логики реализации
> он не содержит (см. `plan/prompt_tochka_vkhoda.md`). Отмечай выполненные пункты
> `[x]`. Спорные места и допущения фиксируй в `plan/faq.md`.

## Этап 0. Подготовка репозитория
- [x] `.gitignore`: исключить `plan/books/` (PDF не коммитим).
- [x] Каркас: `index.html`, `styles.css`, `app.js`, `data/formulas.js` (пустой массив).
- [x] Подключить KaTeX (CDN), проверить рендер тестовой формулы.

## Этап 1. Извлечение контента (по `tz/03_content_source.md`)
- [x] Блок A (стр. 61–67): формулы 1–3 → `data/formulas.js`.
- [x] Блок B (стр. 74–83): формулы 4–11.
- [x] Блок C (стр. 85–91): формулы 12–15.
- [x] Блок D (стр. 94–96): формулы 16–19.
- [x] Сверить каждую формулу с книгой визуально; уточнить номера страниц в карте.
- [x] Заполнить `discovered` и `examples` по внешним источникам (даты — по двум).

## Этап 2. Страница-шпаргалка
- [x] Рендер карточек из `data/formulas.js` (все 7 полей, схема `tz/02_data_model.md`).
- [x] Группировка по блокам A–D + оглавление с якорями.
- [x] Поиск по названию (фильтр по мере ввода).
- [x] Кнопка «скопировать LaTeX» на карточке.

## Этап 3. Вёрстка и адаптив
- [x] Mobile-first стили; проверка на телефоне и ПК (визуальная проверка на
      реальном телефоне — за пользователем, см. faq).
- [x] Читаемость формул (перенос/скролл длинных формул на мобильном).
- [x] Состояние «ничего не найдено» для поиска.

## Этап 4. Публикация
- [x] Включить GitHub Pages, проверить публичный URL.
- [x] README: описание, источник, структура данных, как добавить формулу.
- [x] Финальная проверка Definition of Done из `tz/00_project_summary.md`
      (jsdom-тест: 19 карточек, все поля, поиск, empty-state; прод отдаёт
      19 формул; п. «работает на мобильном» — CSS mobile-first, визуальная
      проверка на реальном устройстве за пользователем).

## Этап 5. Выводы формул (по карте в `tz/03_content_source.md`)
- [x] Схема: опциональное поле `derivation` в `data/formulas.js`
      (шаги `{latex, comment}`, см. `tz/02_data_model.md`).
- [x] Набрать выводы для 7 формул по карте (сверка шагов с книгой):
      сигмоида (из логита), цепочка правдоподобие→J(w), производная сигмоиды
      + правило обновления весов логрегрессии (одной секцией), зазор SVM,
      ядро RBF, числовые примеры энтропии и Джини (сценарии A/B).
- [x] Рендер: свёрнутая секция `<details>` «Как выводится» под формулой;
      у формул без `derivation` секция отсутствует.
- [x] Прогнать LaTeX шагов через KaTeX-проверку; обновить jsdom-тест
      (секция ровно у 7 формул из карты, по умолчанию свёрнута, шаги
      с KaTeX и комментариями).
- [x] Деплой и проверка на проде.
